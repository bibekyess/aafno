# onnxruntime-node (a transitive dependency of @huggingface/transformers, used for Node-side
# tests) tries to download a CUDA-enabled binary by default. Skip it — the spike only needs the
# CPU/WASM backends for Node-side tests; WebGPU is used in the browser instead.
onnxruntime-node-install-cuda=skip
